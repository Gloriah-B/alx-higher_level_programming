def matrix_mul(matrix1, matrix2):
    """
    Multiplies two matrices and returns the result.

    Args:
        matrix1 (list): First matrix represented as a list of lists.
        matrix2 (list): Second matrix represented as a list of lists.

    Raises:
        ValueError: If matrices are not properly formatted for multiplication.

    Returns:
        list: Resultant matrix after multiplication.

    Examples:
    >>> matrix_mul([[1, 2], [3, 4]], [[1, 2], [3, 4]])
    [[7, 10], [15, 22]]

    >>> matrix_mul([[1, 2]], [[3, 4], [5, 6]])
    Traceback (most recent call last):
    ValueError: Matrices are not properly formatted for multiplication
    """
    # Function implementation remains the same as provided
    # ...
